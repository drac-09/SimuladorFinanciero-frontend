import { InputCantidadDirective } from './input-cantidad.directive';

describe('InputCantidadDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCantidadDirective();
    expect(directive).toBeTruthy();
  });
});
