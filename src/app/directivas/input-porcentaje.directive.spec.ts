import { InputPorcentajeDirective } from './input-porcentaje.directive';

describe('InputPorcentajeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPorcentajeDirective();
    expect(directive).toBeTruthy();
  });
});
